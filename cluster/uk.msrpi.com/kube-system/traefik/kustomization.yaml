apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - release.yaml
  - dashboard.yaml

configMapGenerator:
  - name: traefik-config
    files:
      - config/traefik-config.yaml
